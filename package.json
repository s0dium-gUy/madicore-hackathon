{
  "name": "madicore-hackathon",
  "version": "1.0.0",
  "description": "Hospital Queue Management System – Full-Stack MERN",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npx nodemon server.js",
    "dev:frontend": "cd frontend && npx vite",
    "seed": "node seed.js",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
